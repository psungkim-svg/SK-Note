# soonenote v7.5.5 — 수정 내역 (사용자 피드백 4건)

**일시:** 2026-09-14 · **기준본:** v7.5.4 · **작업본:** `/home/user/fix_v7.5.1/`
각 항목을 **확인 → 수정 → 검토(Playwright 실브라우저 E2E)** 순으로 처리.
v7.5.5 신규 47 체크 + v7.5.4 회귀 40 체크 + v7.5.3 회귀 72 체크 전부 통과 (합계 159/159).

---

## 1) Vault 게이트에서 브라우저(구글) 저장된 비밀번호 자동완성 차단

### 확인
- Vault 게이트 비밀번호 입력창 `#vGatePass`가 `autocomplete="current-password"`를 쓰면,
  Chrome/삼성인터넷 등 브라우저가 **이전에 저장해 둔 비밀번호를 자동으로 제안·자동입력**합니다.
- 기기 공유 환경에서 저장된 비밀번호가 자동 입력되고 버튼만 누르면 Vault가 열려버리는
  실질적인 정보유출 경로였습니다.

### 수정
- `#vGatePass` → `autocomplete="new-password"` + `name="sk-vault-gate"`.
  `new-password`는 브라우저의 "저장된 비밀번호 추천" 동작을 무효화하는 표준 속성값이라
  자동완성 제안이 나타나지 않습니다. (설정의 비밀번호 **설정**창 `#pinNew/#pinNew2`는 원래
  `new-password`라 건드리지 않음.)
- 비밀번호 입력을 받는 공용 다이얼로그 `#dlgInput`(현재 비밀번호 확인 등)도
  `autocomplete="new-password"` + `name="sk-dlg-pass"`로 통일.

### 검토
- E2E S1 (11 체크): 게이트 입력창 속성 검증(표시 전·후), PIN 설정 → 잠금 → 재진입 시
  게이트 정상 표시, 비밀번호 입력 → 잠금해제 성공, 재잠금 → 재진입 → 다시 게이트 → 해제.
  (헤드리스 테스트에서는 브라우저 자동완성 UI 자체가 불가하므로, 브라우저가 실제로
  읽는 속성값을 검증 — 속성이 곧 자동완성 제안의 결정 요인.)

---

## 2) 상단 이동 버튼 — 화살표 우측으로, 잘 보이게

### 확인
- v7.5.4의 이동 버튼은 📤(위쪽 화살표) 이모지 + 기본 `.sbtn` 스타일(0.72em, weight 700)로
  휴지통 버튼과 크기가 같아 눈에 띄지 않았고, 화살표 방향도 '위로'였습니다.

### 수정
- 이모지 📤 → **➡️ (우측 화살표)** — 버튼 정적 HTML과 `updateSelBar()` 재생성 문 두 곳.
- 전용 CSS 추가: `#selMode #selMove{font-size:1.08em;font-weight:800;padding:.5em .68em;
  background:rgba(255,255,255,.62);box-shadow:…}` — 휴지통보다 **글자가 크고, 굵고,
  배경이 밝은** 버튼으로 변경.

### 검토
- E2E S2 (8 체크): 아이콘이 ➡ 이고 📤가 없음을 텍스트로 검증, 라벨 '이동' 유지,
  computed style로 font-weight 800·font-size·padding이 휴지통 버튼보다 큰 것 확인.
  이동 기능 자체는 회귀 S5에서 재확인(폴더로 이동 성공).

---

## 3) 이동 대상 화면(리스트) 150% 확대

### 확인
- 이동 버튼을 누르면 열리는 대상 목록(폴더 없음/각 폴더/Vault)의 행 `.fh-trash-row`은
  제목 .79em·메타 .6em의 작은 폰트라 사용자가 "글자도 작다"고 지적.

### 수정
- 이동 대상 모드에서만 `#fhSelectList`에 `move-targets` 클래스를 붙이고
  (노트 선택 목록으로 돌아오면 제거 — 스타일 누설 방지):
  `#fhSelectList.move-targets .fh-trash-row{font-size:1.5em;padding:.62em .7em;gap:.55em;
  border-radius:.75em}` — 행 폰트 1.5배. 행 안의 제목/메타/패딩/간격이 전부 em 기반이라
  **행 전체(글자 포함)가 150%로** 확대됩니다.

### 검토
- E2E S3 (7 체크): 이동 대상 화면에서 행 computed font-size ÷ 목록 기준 font-size = 1.5
  검증, 제목 글자·패딩 확대 확인, Back으로 노트 선택 목록 복귀 시 클래스 제거(누설 없음).

---

## 4) 기본 노트 컬러 7개 → 5개 (마지막 두 개 삭제)

### 확인
- `COLORS/COLORS_D/HEADS_L/HEADS_D/PALETTE_BASE` 7개 + 이름 배열(더스티 로즈, 누드,
  베이비 블루, 세이지, 라벤더, **아이보리, 화이트**). 마지막 두 개 = 아이보리·화이트.

### 수정
- 5개 배열(라이트/다크 본문·제목 톤 포함)과 이름 배열에서 **마지막 두 개 제거** →
  `NCOLORS=COLORS.length`가 자동으로 5.
- 기존 데이터 **자동 이관**(코드 1줄 추가, 별도 이관 로직 불필요):
  - 이미 존재하는 `n.color % NCOLORS` 클램프가 로드 시 **palVer 마이그레이션보다 뒤에서**
    실행되도록 위치를 옮겨 — 5→0, 6→1, 0~4는 그대로.
  - `defColor`(새 노트 기본 컬러)도 범위를 벗어난 5/6이면 0으로.
- 에디터 컬러 팝업·설정 기본컬러 스와치는 원래 배열을 순회해 만들므로 **자동으로 5개**.
- 라벤더(4)는 여전히 색 번호가 가장 커서 '색 순서' 정렬에서 **맨 아래 유지** (논리 변경 없음).
- 글자색 팔레트(`TEXT_COLORS`)·형광펜(`HI_COLORS`)은 별도 기능이라 미변경.

### 검토
- E2E S4 (12 체크): 배열 길이/이름/라벤더 최상위 검증, **실데이터 이관** 검증
  (색 5→0, 6→1, 4 유지, defColor 6→0 — palVer 정상 기기 상태를 그대로 시드로 재현),
  설정 기본컬러 스와치 5개, 에디터 컬러 팝업 스와치 5개.

---

## 공통
- `APP_VER` / Service Worker 캐시 버전을 7.5.5로 승격, 인라인 스크립트 변경으로
  **CSP 내라인 해시를 재계산**(`sha256-YNSGqLIswLSCAl7b4nGNIc3JhZQwF5Nj2vJJtuxEWr8=`) —
  index.html meta와 `DEPLOYMENT_HEADERS.txt` 동일.
- `node --check` 문법 검증, pageerror/console error 전수 스윕 전 suite 통과.
