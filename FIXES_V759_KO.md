# soonenote v7.5.9 — 수정 내역 (사용자 피드백 3건)

**일시:** 2026-09-14 · **기준본:** v7.5.8 · **작업본:** `/home/user/fix_v7.5.1/`
각 항목을 **확인 → 수정 → 검토(Playwright E2E)** 순으로 처리. 신규 17 + 회귀 313 전부 통과.

## 1) 폴더 hub ⋮ — 정렬 · 폴더 한 줄 개수 제거
- **확인:** `renderMore('hub')`가 모든 노트 선택 / 노트 이동 / 정렬(폴더 정렬 3종) / 폴더 한 줄 개수(1–4)를 만들고 있었음.
- **수정:** 정렬·한 줄 개수 블록(하위 선택 포함) 삭제. 기존 정렬/열 설정값은 그대로 유지되어 표시 순서·열 수는 변하지 않음.
- **검토:** 라벨 검사·Back 1단계.

## 2) 폴더 상세 ⋮ — 정렬 · 노트 한 줄 개수 제거
- **확인:** 같은 `renderMore('detail')` 경로(이 폴더 노트 정렬 5종 / 노트 한 줄 개수 1–4).
- **수정:** 1번과 같은 블록이므로 함께 제거. 폴더 이름 변경 · 폴더를 휴지통으로는 유지.
- **검토:** 라벨 검사·Back 1단계.

## 3) T 서식 박스 — 우측 상단 × 제거
- **확인:** `buildTypePop()`이 `#tpClose`(×)를 만들고, v7.5부터 T 재탭은 무시(×·Back만 닫기).
- **수정:** × 생성 코드 삭제. × 없이도 손으로 닫을 수 있게 **T 재탭 = 닫기(histPop 1)** 복원. Back도 그대로 1단계.
- **검토:** `#tpClose` 없음 · 손잡이/B I U 유지 · Back/T 재탭 각각 박스만 닫힘.

## 기타
- `APP_VER='7.5.9'`, sw.js `VERSION="7.5.9"`, CSP `sha256-/SRB9coCTGpP54yG+7TQxH5oh/ssK/Yv1H45N6EGJ8Q=` (meta = DEPLOYMENT_HEADERS.txt).
