# soonenote v7.5.6 — 수정 내역 (사용자 피드백 3건)

**일시:** 2026-09-14 · **기준본:** v7.5.5 · **작업본:** `/home/user/fix_v7.5.1/`
각 항목을 **확인 → 수정 → 검토(Playwright 실브라우저 E2E)** 순으로 처리.
v7.5.6 신규 69 체크 + v7.5.5 48 + v7.5.4 40 + v7.5.3 72 회귀 전부 통과 (합계 229/229).

---

## 1) Vault 게이트 — 브라우저(구글) 저장된 비밀번호 자동완성, 근본 차단  ⭐매우 중요

### 확인
- v7.5.5에서 `autocomplete="new-password"`로 바꿨지만 **실기기 Chrome/구글에서는
  저장된 비밀번호 제안을 막지 못함**(사용자 보고: 여전히 나타남, 누르면 자동 입력돼 열림).
  속성 기반 차단은 브라우저가 실기기에서 우회하는 경우가 있음 — 속성만으로는 해결 불가 판정.

### 수정
- **입력창 자체를 `<input type="password">`에서 contenteditable 마스킹 입력으로 교체.**
  브라우저 자동완성(저장된 비밀번호 제안·자동입력)은 `<input type=password>`에만
  붙는 기능이라, contenteditable이면 **제안 자체가 구조적으로 불가능**합니다.
  - `#vGatePass` = `<div contenteditable="plaintext-only" role="password">` — 기존 입력창과
    동일한 모양(중앙정렬·볼트 표시 ••••, 플레이스홀더, 포커스 링).
  - 입력·백스페이스·단어삭제·붙여넣기를 `beforeinput`으로 직접 처리해 **실제 값은 화면에
    절대 평문으로 드러나지 않음**(항시 •로 표시, 실제 값은 메모리 변수).
  - Enter=확인, 틀리면 자동으로 지워짐, 128자 제한 — 기존 동작 유지.
  - Vault로 노트를 옮길 때의 비밀번호 게이트도 같은 컴포넌트라 함께 적용.

### 검토
- E2E S1 (13 체크): 게이트 필드가 contenteditable DIV 임을 DOM 검증(게이트 안에
  `input[type=password]`가 아예 없음), 키보드 입력→볼트 표시/평문 비노출/백스페이스,
  `fill()` 경로, 잠금해제 성공, 틀린 비밀번호→잠김 유지+입력 지움.
- Vault 세션·게이트 기능 회귀 전부 통과(v7.5.3 72 체크 재실행 포함).

> 참고: 브라우저마다 자동완성 UI는 다르고 헤드리스 테스트에서 실제 드롭다운을
> 재현할 수 없어, "자동완성이 부착될 수 있는 대상(input) 자체가 없는 것"을 검증하는
> 방식입니다. 실기기에서 게이트를 눌러도 비밀번호 제안이 뜨지 않는 것이 목표 동작입니다.

---

## 2) 이동 버튼 — ➡️ 유지, 하얀색 박스 제거

### 확인
- v7.5.5에서 눈에 띄게 하려던 처리(밝은 배경+그림자+크기)가 "테두리 하얀색 박스"로
  느껴졌다는 피드백. 화살표 방향(➡) 자체는 좋다는 피드백.

### 수정
- `#selMode #selMove` 전용 스타일(배경 rgba(255,255,255,.62)·box-shadow·1.08em·800)을
  **전부 제거** — 휴지통 버튼과 동일한 기본 `.sbtn` 스타일로 통일. ➡️ 아이콘·'이동' 라벨은 그대로.

### 검토
- E2E S2: 배경색이 기본 `.sbtn`(rgba(255,255,255,0.35))과 동일, box-shadow 없음,
  크기도 휴지통과 동일, ➡ 유지.

---

## 3) 이동 화면 — 1/3 축소(=원래 크기) + 단계별로 들어가는 구조

### 확인
- v7.5.5의 150% 확대가 "너무 큼" → 1/3 줄이면 원래 크기.
- 기존 이동 화면은 [폴더 없음 / 각 폴더 / Vault]가 한 줄에 펼쳐져 있어
  "첫 화면 → 폴더 → Vault 로 **단계별로 들어가서** 안에 있는 폴더·노트를 보고 선택"하는
  구조가 없다는 피드백.

### 수정
- 150% 스타일(`move-targets` 클래스·1.5em) 제거 — 행은 원래 크기로.
- 이동 화면을 **2단계 드릴다운**으로 재구성:
  - **1단계(루트):** 큰 타일 3개 — 🏠 첫 화면 / 📁 폴더 / 🔐 Vault
    (각각에 현재 개수 표시).
  - **2단계:** 타일을 누르면 그 안의 내용이 나타남.
    - 🏠 첫 화면 → 폴더 없는(첫 화면의) 노트 목록(읽기 전용) + "첫 화면으로 이동(폴더 없음)" 실행 행.
    - 📁 폴더 → 각 폴더 목록(노트 개수 표시) — 폴더를 누르면 그곳으로 이동.
    - 🔐 Vault → **비밀번호 게이트(잠겨 있을 때)** → Vault 안의 내용:
      "Vault(폴더 없음)" 실행 행 + Vault 폴더들(누르면 그 Vault 폴더로 암호화 이동)
      + Vault 노트 목록(읽기 전용). 열려 있으면 게이트 없이 바로 2단계.
- Back 규칙 유지(한 겹씩): 2단계 → 1단계 → 노트 선택 목록 → 폴더 hub → main.
  2단계 진입/이탈은 전용 히스토리 슬롯을 쓰므로 브라우저 뒤로가기도 정확히 한 겹씩.
- 기존 이동 완료 처리(저장·선택 해제·슬롯 정산·Vault 암호화 이동)는 그대로 재사용.

### 검토
- E2E S3 (23 체크): 루트 타일 3개, 150% 제거(행 글자 원래 크기), 폴더 드릴다운→이동,
  첫 화면 드릴다운→폴더 밖 이동, 2단계 노트 목록 표시, Back 4연속(2단계→루트→선택목록→hub→main).
- E2E S4 (17 체크): Vault 잠긴 상태→타일→게이트→해제 후 **Vault 2단계**(Vault 뷰로 바로
  뛰어들지 않음), Vault 폴더 선택→**암호화 이동**(일반 노트에서 제거·Vault 폴더에 복제),
  해제된 상태→게이트 없이 바로 2단계, 2단계 Back 4연속.

---

## 공통
- `APP_VER` / Service Worker 캐시 버전 7.5.6, 인라인 스크립트 변경으로 **CSP 내라인 해시
  재계산**(`sha256-WloE8Dsa47VB1w+NMcPOs/lVxFBJOnx2aURT7A+uDrQ=`) — index.html meta와
  `DEPLOYMENT_HEADERS.txt` 동일.
- `node --check` 문법 검증, 4개 suite(pageerror 0) 전수 통과.
